# misc
.DS_Store
*.vite

# wrangler (mise run preview)
.wrangler/

# secrets for local dev (mise run preview, mise run replies-dev)
.dev.vars

# email worker (workers/replies-inbound)
workers/*/node_modules/
workers/*/.wrangler/
